/**
 * Tailwind CSS v4 uses CSS-first configuration — the real theme lives in
 * src/styles/globals.css (@theme / CSS variables). This file is OPTIONAL in v4
 * and kept only for tooling that still looks for it. Content sources are
 * auto-detected; add overrides here if you ever need them.
 */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
};
